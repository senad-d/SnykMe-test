/**
 * Archived placeholder retained for migration traceability.
 *
 * SnykMe currently wires only command/tool registrations.
 * Optional lifecycle hooks are deferred and intentionally kept out of
 * active extension surface maps in this preparation phase.
 *
 * Archived artifact: `src/archive/lifecycle.ts`
 */
export {};

